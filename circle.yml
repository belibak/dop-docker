machine:
  timezone:
    Europe/Minsk
  services:
    - docker
  
  
dependencies:
 pre:     
    - git clone git@github.com:broadlook-technologies/Data_Optimization_Prototype.git sources
    - pip install docker-compose
    - docker build -t dop-base -f Dockerfile-base . 
    
#test:
#  pre:
#    - bash run-on-circleci.sh
