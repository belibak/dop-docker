machine:
  timezone:
    Europe/Minsk
  services:
    - docker
  
  
dependencies:
 pre:
    - 
    - cd sources && git clone git@github.com:broadlook-technologies/Data_Optimization_Prototype.git && cd ..
    - pip install docker-compose
     
    
test:
  pre:
    - bash run-on-circleci.sh
